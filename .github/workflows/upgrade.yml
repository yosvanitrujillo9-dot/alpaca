name: Upgrade Telar
#
# When a new version of Telar is released, site owners use this workflow to
# upgrade their site. It is triggered manually from the Actions tab in GitHub
# — the user clicks "Run workflow" and the rest is automatic.
#
# How it works (hardened pipeline):
#   1. Resolve the target release tag (the latest release, or an explicit tag
#      passed as an input).
#   2. Download the upgrade tooling for that EXACT tag as a release asset and
#      verify its SHA-256 against the release's published checksums.txt. If the
#      asset is missing or the checksum does not match, the run fails closed and
#      no code is executed. The tooling is extracted into a throwaway temp dir —
#      the site's own scripts/ directory is never deleted or replaced wholesale,
#      so local customisations survive.
#   3. Run the verified engine (scripts/telar_upgrade.py) against the site
#      (--repo-root). Each migration installs its target-version files from the
#      matching release tag (pinned, atomic) and the version is stamped only if
#      every required step succeeds. If anything required fails, the engine exits
#      non-zero, this job goes red, and no upgrade branch or issue is created.
#   4. On success, create an upgrade branch with the content changes (never the
#      fetched tooling or CI bookkeeping files) and open an issue to review and
#      merge.
#
# Version: v1.7.0

on:
  workflow_dispatch:
    inputs:
      target_version:
        description: 'Release tag to upgrade to (e.g. v1.5.0). Leave blank for the latest release.'
        required: false
        type: string

permissions:
  contents: write
  issues: write

env:
  TELAR_REPO: UCSB-AMPLab/telar

jobs:
  upgrade:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0  # Fetch all history for proper git operations

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Resolve target release
        id: resolve
        env:
          # Pass the dispatch input through the environment rather than
          # interpolating it into the script body, so a crafted value cannot
          # inject shell commands into the steps that build URLs, branch names,
          # and the issue body below.
          INPUT_TAG: ${{ github.event.inputs.target_version }}
        run: |
          set -euo pipefail
          if [ -n "$INPUT_TAG" ]; then
            TAG="$INPUT_TAG"
          else
            TAG=$(curl -fsSL "https://api.github.com/repos/${TELAR_REPO}/releases/latest" | jq -r '.tag_name')
          fi
          if [ -z "$TAG" ] || [ "$TAG" = "null" ]; then
            echo "::error::Could not resolve a target release tag for ${TELAR_REPO}."
            exit 1
          fi
          # Refuse anything that is not a plain version tag before it reaches a
          # curl URL, git branch name, or commit/issue body.
          if ! printf '%s' "$TAG" | grep -qE '^v[0-9]+\.[0-9]+\.[0-9]+(-[a-z0-9.]+)?$'; then
            echo "::error::Refusing to proceed: target tag '$TAG' is not a valid version tag (expected vX.Y.Z)."
            exit 1
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "Target release: $TAG"

      - name: Fetch and verify upgrade tooling
        id: fetch
        run: |
          set -euo pipefail
          TAG="${{ steps.resolve.outputs.tag }}"
          TMP="$(mktemp -d)"
          echo "tmp=$TMP" >> "$GITHUB_OUTPUT"

          ASSET="telar-scripts-${TAG}.tar.gz"
          BASE="https://github.com/${TELAR_REPO}/releases/download/${TAG}"

          # Download the verified tooling tarball and its checksums. -f makes a
          # missing asset (e.g. a release published before this mechanism) a
          # hard, fail-closed error rather than a silent empty file.
          echo "Downloading ${ASSET} and checksums.txt for ${TAG}..."
          curl -fsSL -o "$TMP/$ASSET" "$BASE/$ASSET"
          curl -fsSL -o "$TMP/checksums.txt" "$BASE/checksums.txt"

          # Verify SHA-256, fail closed on any mismatch or missing entry.
          EXPECTED=$(grep " ${ASSET}\$" "$TMP/checksums.txt" | awk '{print $1}' | head -n1)
          if [ -z "$EXPECTED" ]; then
            echo "::error::No checksum for ${ASSET} in checksums.txt — refusing to run unverified code."
            exit 1
          fi
          ACTUAL=$(sha256sum "$TMP/$ASSET" | awk '{print $1}')
          if [ "$EXPECTED" != "$ACTUAL" ]; then
            echo "::error::Checksum mismatch for ${ASSET} (expected ${EXPECTED}, got ${ACTUAL})."
            exit 1
          fi
          echo "✓ Verified ${ASSET} (sha256 ${ACTUAL})"

          # Extract the tooling into the temp dir ONLY. The site's working-tree
          # scripts/ is never touched here.
          tar -xzf "$TMP/$ASSET" -C "$TMP"
          # The engine, not the launcher: scripts/upgrade.py in a release is
          # the bootstrap a site runs, and running it here would download the
          # tooling a second time.
          if [ ! -f "$TMP/scripts/telar_upgrade.py" ]; then
            echo "::error::Verified tarball does not contain scripts/telar_upgrade.py."
            exit 1
          fi

      - name: Check current version and language
        id: check_version
        run: |
          set -euo pipefail
          CURRENT_VERSION=$(python3 -c "import yaml; config=yaml.safe_load(open('_config.yml')); print(config.get('telar', {}).get('version', '0.2.0-beta'))")
          echo "current_version=$CURRENT_VERSION" >> "$GITHUB_OUTPUT"
          echo "Current version: $CURRENT_VERSION"

          SITE_LANG=$(python3 -c "import yaml; config=yaml.safe_load(open('_config.yml')); print(config.get('telar_language', 'en'))")
          echo "site_language=$SITE_LANG" >> "$GITHUB_OUTPUT"
          echo "Site language: $SITE_LANG"

      - name: Run upgrade script
        id: upgrade
        run: |
          set -euo pipefail
          TMP="${{ steps.fetch.outputs.tmp }}"

          # Run the verified tooling from the temp dir against the checked-out
          # site. The engine imports its migrations from alongside itself (the
          # verified temp copy); migrations install framework files into the
          # site (--repo-root), pinned to their release tag and written
          # atomically. On any required-step failure the engine exits non-zero,
          # which (with set -e and no continue-on-error) fails this job and
          # prevents the branch/issue steps below from running.
          python "$TMP/scripts/telar_upgrade.py" --repo-root "$GITHUB_WORKSPACE"

          # The engine writes UPGRADE_VERSION.txt only on full success.
          if [ -f UPGRADE_VERSION.txt ]; then
            VERSION=$(cat UPGRADE_VERSION.txt)
            echo "version=$VERSION" >> "$GITHUB_OUTPUT"
            echo "Upgraded to version: $VERSION"
          else
            echo "No upgrade was needed (already up to date)."
          fi

      # NOTE: Version update is handled by the engine using text-based editing
      # to preserve comments in _config.yml. Do NOT use yaml.dump() here as
      # it strips all comments!

      - name: Create upgrade branch
        if: steps.upgrade.outputs.version != ''
        id: create_branch
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          BRANCH_NAME="upgrade-telar-${{ steps.upgrade.outputs.version }}"

          # Delete remote branch if it exists
          git push origin --delete "$BRANCH_NAME" 2>/dev/null || true

          git checkout -b "$BRANCH_NAME"

          # Stage the upgrade's content changes only. The fetched tooling lives
          # in a temp dir outside the working tree, so it is never staged. We
          # additionally exclude the CI bookkeeping files that the engine writes
          # into the site root so they are not committed into the fork's history.
          git add -- . ':!UPGRADE_VERSION.txt' ':!UPGRADE_STATE.json'

          git commit -m "Upgrade Telar from ${{ steps.check_version.outputs.current_version }} to ${{ steps.upgrade.outputs.version }}"
          git push origin "$BRANCH_NAME"

          echo "branch_name=$BRANCH_NAME" >> "$GITHUB_OUTPUT"

      - name: Create upgrade issue
        if: steps.create_branch.outputs.branch_name != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Get repository info
          REPO="${{ github.repository }}"
          BRANCH="${{ steps.create_branch.outputs.branch_name }}"
          VERSION="${{ steps.upgrade.outputs.version }}"
          FROM_VERSION="${{ steps.check_version.outputs.current_version }}"
          SITE_LANG="${{ steps.check_version.outputs.site_language }}"
          COMPARE_URL="https://github.com/$REPO/compare/main...$BRANCH"

          # Strip YAML frontmatter from UPGRADE_SUMMARY.md (remove everything between first --- and second ---)
          SUMMARY_CONTENT=$(sed '/^---$/,/^---$/d' UPGRADE_SUMMARY.md)

          # Extract automated changes and manual steps counts from the summary
          AUTOMATED_CHANGES=$(grep -o '\*\*Automated changes:\*\* [0-9]\+' UPGRADE_SUMMARY.md | sed 's/.*\*\* \([0-9]\+\)/\1/')
          MANUAL_STEPS=$(grep -o '\*\*Manual steps:\*\* [0-9]\+' UPGRADE_SUMMARY.md | sed 's/.*\*\* \([0-9]\+\)/\1/')

          # Extract manual steps from UPGRADE_SUMMARY.md (if any)
          # Keep interior blank lines so multi-paragraph manual steps render as
          # separate paragraphs in the GitHub issue (do not strip /^$/).
          MANUAL_STEPS_TEXT=$(sed -n '/## Manual Steps Required/,/## Resources/p' UPGRADE_SUMMARY.md | sed '1d;$d' | sed '/^Please complete these after merging:/d')

          # Set language-specific strings
          if [ "$SITE_LANG" = "es" ]; then
            TITLE="Actualizar Telar a $VERSION"
            SUPERSEDED_MSG="Reemplazado por una ejecucion mas reciente del workflow de actualizacion."
            HEADER="## Actualizacion lista: v$FROM_VERSION → v$VERSION"
            STATUS="**Estado:** Actualizacion automatica completada"
            BRANCH_LABEL="**Rama:**"
            CHANGES_LABEL="**Cambios:** $AUTOMATED_CHANGES automaticos, $MANUAL_STEPS manuales"
            NEXT_STEP="## Siguiente paso: Revisar y fusionar"
            INTRO="Tu sitio Telar ha sido actualizado exitosamente y esta listo para fusionar."
            CTA="### **[Haz clic aqui para revisar los cambios y crear pull request]($COMPARE_URL)**"
            CTA_FOLLOW="Luego haz clic en el boton verde \"Create pull request\", revisa los cambios y fusiona para completar la actualizacion."
            CLI_SUMMARY="<b>Para usuarios que prefieren la linea de comandos</b> (clic para expandir)"
            CLI_INTRO="Si prefieres trabajar desde tu terminal:"
            CLI_NOTE="Esto fusionara la rama de actualizacion en tu rama principal. Recomendamos usar el metodo de pull request de arriba, ya que proporciona una mejor interfaz de revision."
            AFTER_MERGE="## Despues de fusionar"
            AFTER_INTRO="Una vez que hayas fusionado la actualizacion, por favor completa lo siguiente:"
            MANUAL_HEADER="### Pasos manuales"
            VERIFY_HEADER="### Verifica tu sitio"
            VERIFY_STEPS="Despues de que la actualizacion se complete y tu sitio se reconstruya:

          1. Visita tu sitio en vivo y navega por algunas paginas
          2. Busca mensajes de advertencia o alertas sobre problemas de configuracion
          3. Si ves advertencias, sigue los enlaces o documentacion proporcionada para resolverlas
          4. Prueba las funciones clave como navegacion de historias, visualizacion de imagenes y terminos del glosario
          5. **Cierra este issue** una vez que todo este funcionando - esto marca la actualizacion como completa"
            WHAT_CHANGED="## Que cambio"
            CHANGES_SUMMARY="<b>Ver cambios automaticos por categoria</b> (clic para expandir)"
            FOOTER="<sub>Esta actualizacion fue realizada automaticamente por el workflow de actualizacion de Telar.</sub>"
          else
            TITLE="Upgrade Telar to $VERSION"
            SUPERSEDED_MSG="Superseded by a fresh run of the upgrade workflow."
            HEADER="## Upgrade Ready: v$FROM_VERSION → v$VERSION"
            STATUS="**Status:** Automated upgrade complete"
            BRANCH_LABEL="**Branch:**"
            CHANGES_LABEL="**Changes:** $AUTOMATED_CHANGES automated, $MANUAL_STEPS manual"
            NEXT_STEP="## Next Step: Review and Merge"
            INTRO="Your Telar site has been successfully upgraded and is ready to merge."
            CTA="### **[Click here to review changes and create pull request]($COMPARE_URL)**"
            CTA_FOLLOW="Then click the green \"Create pull request\" button, review the changes, and merge to complete the upgrade."
            CLI_SUMMARY="<b>For users who prefer the command line</b> (click to expand)"
            CLI_INTRO="If you prefer working from your terminal:"
            CLI_NOTE="This will merge the upgrade branch into your main branch. We recommend using the pull request method above instead, as it provides a better review interface."
            AFTER_MERGE="## After Merging"
            AFTER_INTRO="Once you've merged the upgrade, please complete the following:"
            MANUAL_HEADER="### Manual Steps"
            VERIFY_HEADER="### Verify Your Site"
            VERIFY_STEPS="After the upgrade completes and your site rebuilds:

          1. Visit your live site and navigate through a few pages
          2. Check for any warning messages or alerts about configuration issues
          3. If you see any warnings, follow the links or documentation provided to resolve them
          4. Test key features like story navigation, image viewing, and glossary terms
          5. **Close this issue** once everything is working - this marks the upgrade as complete"
            WHAT_CHANGED="## What Changed"
            CHANGES_SUMMARY="<b>View automated changes by category</b> (click to expand)"
            FOOTER="<sub>This upgrade was performed automatically by the Telar upgrade workflow.</sub>"
          fi

          # Create issue body
          cat > issue_body.md << EOF
          $HEADER

          $STATUS
          $BRANCH_LABEL \`$BRANCH\`
          $CHANGES_LABEL

          ---

          $NEXT_STEP

          $INTRO

          $CTA

          $CTA_FOLLOW

          <details>
          <summary>$CLI_SUMMARY</summary>

          $CLI_INTRO

          \`\`\`bash
          git fetch origin
          git checkout main
          git merge origin/$BRANCH
          git push origin main
          \`\`\`

          $CLI_NOTE

          </details>

          ---

          $AFTER_MERGE

          $AFTER_INTRO

          EOF

          # Add manual steps section only if there are manual steps
          if [ "$MANUAL_STEPS" != "0" ] && [ -n "$MANUAL_STEPS_TEXT" ]; then
            cat >> issue_body.md << EOF
          $MANUAL_HEADER

          $MANUAL_STEPS_TEXT

          EOF
          fi

          # Always add verify section
          cat >> issue_body.md << EOF
          $VERIFY_HEADER

          $VERIFY_STEPS

          ---

          $WHAT_CHANGED

          <details>
          <summary>$CHANGES_SUMMARY</summary>

          $SUMMARY_CONTENT

          </details>

          ---

          $FOOTER
          EOF

          # Create upgrade label if it doesn't exist
          gh label create "upgrade" --description "Automated Telar upgrade" --color "0E8A16" 2>/dev/null || true

          # Close any existing open issue with the same title as the one this
          # run is about to create, so re-running the workflow does not leave
          # duplicate upgrade issues open for the same version and language.
          # $TITLE is passed via --arg rather than interpolated into the jq
          # filter, consistent with the injection-safety approach used above.
          EXISTING_ISSUE=$(gh issue list --state open --label upgrade --json number,title | \
            jq -r --arg title "$TITLE" '.[] | select(.title == $title) | .number' | head -n1)
          if [ -n "$EXISTING_ISSUE" ]; then
            gh issue comment "$EXISTING_ISSUE" --body "$SUPERSEDED_MSG"
            gh issue close "$EXISTING_ISSUE"
          fi

          # Create the issue
          gh issue create \
            --title "$TITLE" \
            --body-file issue_body.md \
            --label "upgrade"

      - name: Output summary
        if: steps.create_branch.outputs.branch_name != ''
        run: |
          echo "## ✅ Upgrade Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **From:** ${{ steps.check_version.outputs.current_version }}" >> $GITHUB_STEP_SUMMARY
          echo "- **To:** ${{ steps.upgrade.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** \`${{ steps.create_branch.outputs.branch_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "An issue has been created with upgrade details and merge instructions." >> $GITHUB_STEP_SUMMARY
          echo "Check the **Issues** tab to review and apply the upgrade." >> $GITHUB_STEP_SUMMARY
